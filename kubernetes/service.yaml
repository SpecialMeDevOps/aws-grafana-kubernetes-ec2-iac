apiVersion: v1
kind: Service
metadata:
  name: aws-grafana-demo
  namespace: aws-grafana
spec:
  type: NodePort
  selector:
    app: aws-grafana-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
